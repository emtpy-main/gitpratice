// Utility functions for Git practice

function greet(name) {
    return `Hello, ${name}!`;
}

function square(num) {
    return num * num;
}

function isEven(num) {
    return num % 2 === 0;
}

module.exports = { greet, square, isEven };